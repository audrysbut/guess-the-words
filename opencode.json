{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "notify": {
      "description": "Trigger a desktop notification (sound + popup)",
      "template": "Run the notification script: bash scripts/notify.sh 'Guess the Words' 'Task complete: $ARGUMENTS'"
    }
  }
}
